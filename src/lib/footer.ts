// Single source of truth for the site-wide copyright line.
// Used by CareersFooterSection (home stack) and LegalFooter (legal pages).
export const COPYRIGHT_LINE = (year: number) =>
  `© ${year} v7m.org. Todos os direitos reservados.`